- name: Install root deps
  run: npm ci

- name: Build root app
  run: npm run build

- name: Install second deps
  run: npm ci --prefix second

- name: Build second app
  run: npm run build --prefix second

- name: Combine outputs for Pages
  run: |
    mkdir -p dist/second
    cp -r second/dist/* dist/second/
